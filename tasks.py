def edges_list_to_adjacency_matrix(
    vertex_count: int, edges: list[tuple[int, int]], directed: bool
) -> list[list[int]]:
    """Задание 1: преобразовать список ребер в матрицу смежности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    vertex_count -- количество вершин в графе.
    edges -- список ребер. Вершины в ребрах пронумерованы с 0:
        например, ребро (0, 2) соединяет вершины 0 и 2.
    directed -- True, если граф ориентированный, иначе False.

    Нужно вернуть квадратную матрицу смежности размера vertex_count x vertex_count:
    - matrix[i][j] == 1 означает ребро или дугу из вершины i в вершину j;
    - для неориентированного графа матрица должна быть симметричной.
    """
    raise NotImplementedError("Функция не реализована.")


def adjacency_matrix_to_adjacency_list(
    adjacency_matrix: list[list[int]], directed: bool
) -> list[list[int]]:
    """Задание 2: преобразовать матрицу смежности в список смежности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_matrix -- квадратная матрица смежности.
    directed -- True, если граф ориентированный, иначе False.

    Нужно вернуть список смежности с нумерацией вершин с 0:
    - result[0] содержит соседей вершины 0;
    - result[1] содержит соседей вершины 1;
    - значения внутри списков тоже являются номерами вершин с 0.
    """
    raise NotImplementedError("Функция не реализована.")


def adjacency_list_to_adjacency_matrix(
    adjacency_list: list[list[int]], directed: bool
) -> list[list[int]]:
    """Задание 3: преобразовать список смежности в матрицу смежности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_list -- список смежности с нумерацией вершин с 0:
        adjacency_list[0] содержит соседей вершины 0.
    directed -- True, если граф ориентированный, иначе False.

    Нужно вернуть квадратную матрицу смежности:
    - matrix[i][j] == 1 означает ребро или дугу из вершины i в вершину j;
    - для неориентированного графа матрица должна быть симметричной.
    """
    raise NotImplementedError("Функция не реализована.")


def depth_first_search(adjacency_list: list[list[int]], start_vertex: int) -> list[int]:
    """Задание 4: выполнить поиск в глубину.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_list -- список смежности с нумерацией вершин с 0.
    start_vertex -- номер вершины, с которой нужно начать обход.

    Нужно вернуть список вершин в порядке обхода.
    """
    raise NotImplementedError("Функция не реализована.")


def connected_components(adjacency_list: list[list[int]]) -> list[list[int]]:
    """Задание 5: найти компоненты связности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_list -- список смежности с нумерацией вершин с 0.

    Нужно вернуть список компонент связности.
    Каждая компонента -- это список номеров вершин.
    """
    raise NotImplementedError("Функция не реализована.")


def breadth_first_search(adjacency_list: list[list[int]], start_vertex: int) -> list[int]:
    """Задание 6: выполнить поиск в ширину.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_list -- список смежности с нумерацией вершин с 0.
    start_vertex -- номер вершины, с которой нужно начать обход.

    Нужно вернуть список вершин в порядке обхода.
    """
    raise NotImplementedError("Функция не реализована.")


def shortest_path(
    adjacency_list: list[list[int]], start_vertex: int, end_vertex: int
) -> list[int]:
    """Задание 7: найти кратчайший путь в графе.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_list -- список смежности с нумерацией вершин с 0.
    start_vertex -- номер начальной вершины.
    end_vertex -- номер конечной вершины.

    Нужно вернуть список вершин на кратчайшем пути от start_vertex до end_vertex.
    Если пути нет, верните пустой список.
    """
    raise NotImplementedError("Функция не реализована.")
