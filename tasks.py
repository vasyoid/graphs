def edges_list_to_incidence_matrix(
    vertex_count: int, edges: list[tuple[int, int]], directed: bool
) -> list[list[int]]:
    """Задание 1: преобразовать список ребер в матрицу инцидентности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    vertex_count -- количество вершин в графе.
    edges -- список ребер. Вершины в ребрах пронумерованы с 1:
        например, ребро (1, 3) соединяет вершины 1 и 3.
    directed -- True, если граф ориентированный, иначе False.

    Нужно вернуть матрицу инцидентности:
    - количество строк равно vertex_count;
    - количество столбцов равно количеству ребер;
    - для неориентированного графа столбец ребра содержит две единицы;
    - для ориентированного графа используйте -1 для начала дуги и 1 для конца дуги.
    """
    raise NotImplementedError("Функция не реализована.")


def incidence_matrix_to_adjacency_list(
    incidence_matrix: list[list[int]], directed: bool
) -> list[list[int]]:
    """Задание 2: преобразовать матрицу инцидентности в список смежности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    incidence_matrix -- матрица инцидентности.
    directed -- True, если граф ориентированный, иначе False.

    Нужно вернуть список смежности с нумерацией вершин с 0:
    - result[0] содержит соседей вершины 0;
    - result[1] содержит соседей вершины 1;
    - значения внутри списков тоже являются номерами вершин с 0.
    """
    raise NotImplementedError("Функция не реализована.")


def adjacency_list_to_incidence_matrix(
    adjacency_list: list[list[int]], directed: bool
) -> list[list[int]]:
    """Задание 3: преобразовать список смежности в матрицу инцидентности.

    Эту функцию нужно реализовать самостоятельно.

    Аргументы:
    adjacency_list -- список смежности с нумерацией вершин с 0:
        adjacency_list[0] содержит соседей вершины 0.
    directed -- True, если граф ориентированный, иначе False.

    Нужно вернуть матрицу инцидентности:
    - количество строк равно количеству вершин;
    - количество столбцов равно количеству ребер;
    - для неориентированного графа столбец ребра содержит две единицы;
    - для ориентированного графа используйте -1 для начала дуги и 1 для конца дуги.
    """
    raise NotImplementedError("Функция не реализована.")
